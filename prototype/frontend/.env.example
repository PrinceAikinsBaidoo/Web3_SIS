NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_RECORD_REGISTRY_ADDRESS=
NEXT_PUBLIC_STUDENT_IDENTITY_ADDRESS=
NEXT_PUBLIC_VERIFICATION_LOG_ADDRESS=
NEXT_PUBLIC_INSTITUTION_REGISTRY_ADDRESS=

# Pinata — client upload (legacy; avoid in production; secrets leak to browser)
NEXT_PUBLIC_PINATA_API_KEY=
NEXT_PUBLIC_PINATA_SECRET_KEY=
NEXT_PUBLIC_PINATA_JWT=

# Pinata — server-only (recommended for POST /api/ipfs/unpin “Revoke + unpin” erasure)
PINATA_JWT=
# Or API key pair (server-side names)
PINATA_API_KEY=
PINATA_SECRET_API_KEY=